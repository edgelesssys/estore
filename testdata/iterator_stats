build ext1
merge a 1
set c 2
----

ingest ext1
----
6:
  000004:[a#10,MERGE-c#10,SET]

iter
first
next
next
stats
----
a: (1, .)
c: (2, .)
.
stats: (interface (dir, seek, step): (fwd, 1, 2), (rev, 0, 0)), (internal (dir, seek, step): (fwd, 1, 2), (rev, 0, 0)),
(internal-stats: (block-bytes: (total 56B, cached 56B, read-time 0s)), (points: (count 2, key-bytes 2B, value-bytes 2B, tombstoned 0)))

# Perform the same operation again with a new iterator. It should yield
# identical statistics.

iter
first
next
next
stats
----
a: (1, .)
c: (2, .)
.
stats: (interface (dir, seek, step): (fwd, 1, 2), (rev, 0, 0)), (internal (dir, seek, step): (fwd, 1, 2), (rev, 0, 0)),
(internal-stats: (block-bytes: (total 56B, cached 56B, read-time 0s)), (points: (count 2, key-bytes 2B, value-bytes 2B, tombstoned 0)))

build ext2
set d@10 d10
set d@9 d9
set d@8 d8
set e@20 e20
set e@18 e18
----

ingest ext2
----
6:
  000004:[a#10,MERGE-c#10,SET]
  000005:[d@10#11,SET-e@18#11,SET]
